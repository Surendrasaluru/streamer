import React from "react";

import VideoContainer from "./VideoContainer";

const MainContainer = () => {
  return (
    <div className="col-span-11 dark:text-slate-100 dark:bg-black">
      <VideoContainer />
    </div>
  );
};

export default MainContainer;
